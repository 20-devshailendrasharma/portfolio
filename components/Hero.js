import React, { Component } from 'react';
import HeroCarousel from './HeroCarousel';

const Hero = () => {
    return (
        <div>
            <HeroCarousel />
            {/* Other content */}
        </div>
    );
}

export default Hero;

